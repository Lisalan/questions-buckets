<!DOCTYPE html>
<html>
<head>
  <title>Bucket Questions</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
    <header class="navbar navbar-fixed-top navbar-inverse">
      <div class="container">
        <%= link_to "Virtual Patient Conversation Store", static_pages_home_path, id: "logo" %>
        <nav>
          <ul class="nav navbar-nav pull-right">
            <li><%= link_to "Create Unmatched Questions", new_question_path %></li>
            <li><%= link_to "Un/Non Matched Questions", questions_path %></li>
            <li><%= link_to "Bucket & Questions", buckets_path %></li>
            <li><%= link_to "Search Bucket", search_buckets_path %></li>
          </ul>
        </nav>
      </div>
    </header>
    <div class="container">
      <% flash.each do |message_type, message| %>
        <div class="alert alert-<%= message_type %>"><%= message %></div>
      <% end %>
      <%= yield %>
      <%=render 'layouts/footer'%>
    </div>
</body>
</html>
