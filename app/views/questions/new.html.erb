<h1> Create Unmatched Question </h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@question) do |f| %>
     <%= f.text_field :content %>
     <%= f.submit "Create Unmatched Question", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>